apiVersion: v1
kind: Service
metadata:
  name: neuroscale
  labels:
    app.kubernetes.io/name: neuroscale
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: neuroscale
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
